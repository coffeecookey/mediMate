# No environment variables are currently required to run this chatbot client.
#
# The backend base URL is hardcoded as http://localhost:4000 in:
#   - src/App.jsx
#
# This file exists as a placeholder so the convention matches backend/env.example.
# If a configurable API base URL is introduced later, add it here as:
# VITE_API_BASE_URL=http://localhost:4000
